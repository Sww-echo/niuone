# app 目录约定

`app/` 根目录只保留包声明和本说明文件。可执行入口统一位于 `entrypoints/`，历史裸模块适配器统一位于 `compat/`，实际实现按领域存放：

| 领域目录 | 实现内容 |
|---|---|
| `automation/` | Cron 规则与调度服务 |
| `backtesting/` | 独立的多源历史日 K、服务端持久任务、选股信号与策略买卖回放；不被模拟交易加载 |
| `compat/` | 历史裸模块名适配器，不承载业务实现 |
| `core/` | 路径、缓存等基础设施 |
| `dashboard/` | Dashboard 服务、公开投影、版本化快照、API、安全辅助、行情采样与页面侧任务编排 |
| `entrypoints/` | Dashboard、调度器、监控与报告等可执行入口 |
| `market_data/` | 行情访问、证券代码规范化、日 K/量能缓存与问财研究数据客户端 |
| `messaging/` | 通知模型、渠道、分发与兼容层 |
| `monitoring/news/` | NewsNow 金融快讯客户端、规范化、缓存与失败降级 |
| `reports/a_share/` | A 股竞价、午盘、盘后、日历与 Grok 报告 |
| `reports/us/` | 隔夜美股盘面报告 |
| `screening/` | 多策略/全市场扫描、候选增强与题材强度缓存 |
| `storage/` | 推送历史、模拟盘数据库、迁移与报告存储 |
| `strategies/` | 策略注册、评分、归因、风险预算、Prompt 与退出规则 |
| `trading/` | 模拟账户、模型决策、执行风控、优化器和卖出信号 |

`backtesting/` 的历史 K 线按东方财富、腾讯、新浪顺序降级；新浪仅支持不复权模式，前/后
复权回测因此只使用东方财富和腾讯。牛牛题材优先使用东方财富 `f103` 多概念归属，板块潮汐
优先使用东方财富 `f100` 行业；分类批量响应会原子保存为私有日快照。刷新失败时优先复用已
校验的东方财富旧快照。仅在完全没有有效东方财富快照时，已启用且配置密钥的问财数据源才可
使用完整分页并校验过的同花顺行业/概念快照冷启动；两套分类口径独立缓存且不混合补洞。两者
均不可用时，回测明确失败。
Dashboard 的“今日前5”另读取东方财富概念板块涨幅榜前 100，按规范化后的精确题材名展示即时
排名、上涨广度和领涨股，仅用于交叉验证。该快照采用分钟级进程缓存、短时陈旧回退和失败退避；
它不参与牛牛题材归因、今日强度排序、跨日确认、回测或交易门槛，接口失败时原排名照常生成。
分钟题材重算及大型缓存序列化通过 `entrypoints/niuone_minute_refresh.py` 在有界隔离进程执行，
Dashboard 主进程继续提供上一份有效读模型；行情快照在等待期内只保留最新一份，并按耗时自适应降载。
完整选股、独立题材研究或日 K 预热运行时，分钟重算自动让行并在任务结束后消费最新快照，避免重型任务叠加。
管理员回测由单任务、低优先级独立子进程执行，结束后由操作系统回收行情与题材截面内存；
Dashboard 只负责持久化状态和结果。选股回放带按协议、日期、行情源、复权方式、股票池、
分类快照及实际历史行情指纹缓存，执行参数变化可复用选股结果；缓存损坏或写入失败时自动重算。
冻结文字策略由 `backtesting/prompt_strategy.py` 专用状态机回放；缓存额外绑定版本 ID、计划
SHA-256 和 `prompt-backtest-v2`。v2 将信号期后的最终平仓日纳入净值曲线与风险指标，旧
v1 结果不再恢复。它使用独立资金逐日执行选股、次日开盘买入、持仓监测和
收盘卖出，并把可重放审计保存在回测结果中，不写模拟账户或生产文字策略审计库。
牛牛多概念股票按试仓、启动、领涨、转强各自选择生命周期相容的题材画像；主/次主线是排序展示字段，不是只允许两个题材交易的资格门槛。回测拒绝诊断按自然月保留评分阈值敏感性、硬门槛逐项/分族消融和龙头分支覆盖。

牛牛严格前向证据沿用模拟账户真实成交链路：Cron Scheduler 启动时及工作日首轮决策前先冻结或校验协议，完整成交 payload 与每轮完整观察候选/过滤结果幂等持久化在私有 SQLite。成交、决策、日内权益和日度权益历史先以不可变版本追加到 SQLite，归档事务成功后账户 JSON 才有界保留当前状态和近期记录；盘后评估器只读重建零仓到零仓生命周期。Scheduler 与 Dashboard 分别有界保留每日任务结果和每个 Practice 决策槽的终态；协议 v20 还逐槽核对耐久决策账本，并按五阶段输出“观察候选→进入决策池→模型 BUY→实际 BUY”的去重决策周期漏斗及定仓利用率。牛牛新开仓不再按上午/下午、单轮或单日计数，持仓只数上限统一读取设置中的“最大持仓只数”，并继续执行原组合风险预算；加仓继续作为已有持仓的独立 BUY 审计。空候选池是有效观察；延迟成交继承原槽机会集，字段残缺或内部矛盾的证据不能满足槽账本。只有预检早于首槽、全部配置槽都完成扫描与决策且落盘、两次独立退出检查、15:15 盘后账户净值快照和 15:20 评估均成功的 A 股运行日才算完整。首次入场会同时冻结按题材前态因果推进的主线五阶段代码、中文标签、顺序、入口策略和行业；持仓期间每次主线扫描继续记录紧凑阶段路径，真实 SELL 再冻结退出阶段。报告按入口阶段、退出阶段、完整路径和行业分组，并要求每个实际运行日都有阶段观察。队列起始日前允许同一起始日的预锁保留旧指纹审计后自动重冻，起始日当天及以后仍禁止覆盖漂移锁。只有全部已完成生命周期都具备完整入口与持仓路径归因、且队列内运行覆盖率为 100% 时，交易数/时间门才可进入人工复核；旧账本、阶段错配、状态层独有成交或运行日缺口都不能推动策略晋级。
协议同时冻结五阶段与四动作的唯一入口路由：酝酿→试仓，主升→启动/领涨，高潮→满足条件的领涨或企稳转强，分歧→仍有效的领涨或企稳收复后的转强，退幕→只退出。牛牛试仓首次或清仓后重新开仓要求实际执行价较有效前收盘价的涨幅严格小于 3%，缺少有效报价也拒绝；其他入口保留原规则，所有买单仍复核涨停价；评分、决策候选、成交前复核、历史回放和盘后证据门共用该路由，其他阶段、形态与风险错配仍不能进入可交易池。
v20 漏斗中的“实际 BUY”来自耐久成交账本，并与决策 payload 的执行副本交叉核对，不以单一副本替代真实成交证据；存在结构缺口或交叉账本不一致时同样保持 `data_quality_blocked`。协议锁还冻结起始日前的零持仓账户基线、每日牛牛新仓数值与计数口径；队列内只要出现非牛牛/未知策略成交，任一实际运行日缺少持仓阶段观察或耐久盘后权益点，组合收益与回撤证据就失败关闭。满三个月但不足 30 笔只开放频率/运营复核；成效声明还须同时通过冻结历史胜率参考、交易级 Wilson 95% 下界、按首次入场日期×行业计算的唯一簇数和 Herfindahl 有效簇数、簇等权胜率及其 95% 下界、费用后生命周期收益、正组合收益、最大回撤不超过 6%、收益/回撤不低于 1，以及运行日与机会漏斗完整性，且绝不自动晋级。

v18 同时冻结牛牛 BUY 与模型 SELL 的安全裁单语义：BUY 若已通过资格、容量和风险输入检查，只是 100 股整手请求高于正数确定性风险上限，则按上限成交；模型 SELL 若请求高于正数整手 T+1 可卖量，则按可卖量成交。SELL 的模型请求、当时可卖量、实际股数和裁单标记进入耐久成交并由盘后质量门校验；需要裁单但可卖量为零或非整手时仍拒单，系统自动退出与其他战法维持原执行语义。
v18 还冻结牛牛试仓日线 V 型恢复比 `[0.60, 2.00)`：不足六成视为右侧确认不完整，达到两倍则已超出早期试仓语义；评分和成交前复核共用同一失败关闭规则。冻结历史参考胜率同步更新为 59.29%，仍须由未见前向样本证明，不自动宣称未来改善。
v18 同时冻结主升质量边界：牛牛领涨必须处于主线内前 20% 且题材当日强度不低于 60；牛牛启动只允许跨日延续的 `emerging`，已确认 `mainline` 必须改走领涨。三项规则在评分和成交前都失败关闭。
v18 进一步冻结酝酿试仓的延续质量与资金边界：题材强势股数必须不少于 6，或酝酿状态至少连续 3 个交易日；每天最多保留 2 个合格试仓，单票绝对仓位上限为 6.25%。评分与成交前复核共用延续 blocker，进攻/轮动/修复的单笔风险预算仍为 0.35%/0.30%/0.25%。
v20 将阶段化资金路由拆成两级：6.25% 只约束酝酿试仓；原试仓或启动仓浮盈处于 2%～12%、仍处主升且仍为强势领涨时，启动主线跨日延续先在下一交易时段向 10% 上限加仓一次，主线完全确认后再向 20% 上限加仓一次。实际目标始终取风险定仓、主题/组合容量、现金和阶段上限的最小值；浮盈超过 12% 不追仓，高潮、分歧、退幕不加仓。首次进入高潮且持仓不亏时减仓 1/3 一次，原分段止盈、成本保护与 2ATR 跟踪保持。

v21 把确认领涨后的仓位管理改为可重复的波段再平衡，而不是再增加一个固定加仓次数：距本轮收盘高点回落 1 ATR，或至少连续 3 个交易日未创新高且回落不少于 0.25 ATR 时，先减仓 1/3；随后只有价格从减仓价重新上行 0.5 ATR、生命周期回到主升且个股恢复强势领涨，才补回风险预算允许的目标仓位。每次补仓都会重置周期，必须等待下一次独立回撤才可再次减仓/加仓，因此终身次数不设上限；高潮、未修复分歧和退幕仍禁止加仓。严格前向协议同步升级为 `niuone-strict-forward-v21`。
v22 修复多概念动作路由和分歧领涨阶段错配：四个动作各自选择相容题材画像，页面主/次主题不再限制其他已确认分支，`diverging` 强势核心领涨不再被 `mainline` 状态和题材当日强度 60 的矛盾门槛二次阻断。组合容量、价格形态和结构风控保持；严格前向协议升级为 `niuone-strict-forward-v22`，默认新队列从 `2026-08-04` 开始。
v23 为已进入主升、仍处 `emerging` 且完成跨日延续的行业龙一增加“主升动量试仓”子路由：仅接受强度不低于 90、评分不低于 8.0、非防守行情且次日执行高开不超过 3% 的候选。该路由可容纳不超过 3.2ATR、18%/3ATR 的价格与结构空间，但首仓绝对上限固定为 3%，并继续按有效亏损距离缩仓；普通启动、反转与领涨门槛不变。严格前向协议升级为 `niuone-strict-forward-v23`。
v24 收紧主升动量试仓的中间追高区：普通路径要求评分不低于 8.1、题材分不低于 70 且距 EMA20 不超过 1ATR；2.5～3.2ATR 只保留单日涨幅不低于 9.5%、量比不高于 1.2 的极强加速例外，继续覆盖 600206 型龙头。高质量试仓绝对上限由 3% 调整为 4%，实际仓位仍受有效亏损距离风险预算约束。严格前向协议升级为 `niuone-strict-forward-v24`。
管理员回测 v25 固定使用进取风险参数，不再对外提供均衡/进取选择；旧客户端即使传入 `balanced` 也会被强制归一化为 `aggressive`。回测协议升级为 `niuone-backtest-v25`，旧协议或旧均衡结果不再恢复；分支报告继续把“最佳标的当日阻断”与“月度累计阻断族”分开。进取参数仅放大回测账户风险、仓位敞口和持仓容量，不改变子路由身份条件或实盘/Practice 组合风控。
v25 为已经完成高潮减仓、个股仍强且题材分不低于 55、未进入退幕的领涨余仓增加条件化主升跟随：相对龙头排名退出由连续 2 日改为 3 日确认，ATR 跟踪线由 2ATR 放宽至 3ATR。未减仓仓位、弱势个股和衰退题材仍沿用原两日/2ATR 风控，结构止损、成本保护、主线转弱和市场硬停止不放宽。严格前向协议升级为 `niuone-strict-forward-v25`；管理员回测同步升级为 `niuone-backtest-v26`。
v26 将防守状态从“零预算禁开仓”改为牛牛最低风险档：成熟路径单笔/组合/主题风险上限为 0.30%/0.90%/0.60%，总仓/主题敞口为 20%/12%；试仓进一步收紧为 0.15% 单笔风险、0.30% 主题风险和 5% 主题敞口。防守状态可开仓，复合风险硬停止仍只允许卖出或持有。严格前向协议升级为 `niuone-strict-forward-v26`，管理员回测同步升级为 `niuone-backtest-v27`。
v27 将股票的东方财富 `f100` 事实行业与牛牛动作选中的 `f103` 题材拆开保存：候选的 `industry/sector` 只表示行业，`signal_theme` 表示本轮入选题材。多概念归因由 75% 当前题材/个股共振证据和 25% 前序快照历史先验组成，所有概念权重归一化为 1；首次建仓冻结 `entry_theme` 及归因依据，持仓只有在另一有效生命周期题材连续 2 个交易日领先至少 10 分后才切换 `active_theme`。题材容量和风险按动作/有效题材计算，页面同时显示题材与行业。严格前向协议升级为 `niuone-strict-forward-v27`，按首次入场日期×入场题材聚类并要求完整题材归因；管理员回测同步升级为 `niuone-backtest-v28`，旧协议证据不得混算。
v29 将 `f103` 明确降级为题材候选集：个股只按排除自身后的同题材共振、同群方向、当日排名和结构排名形成当前归因证据，再叠加前序先验；题材识别不发起消息面检索，消息摘要不会新增候选、修改归因分或改变题材总分。多概念使用 softmax 分配并保留未归因质量，避免弱标签被强制分满。题材强股、成交额、广度和领涨股随后按归因权重重算，今日广度按有效归因样本向全市场广度收缩；低于 15% 归因权重的股票不得成为该题材领涨股，Dashboard 折叠由同一核心群驱动的标签副本。独立题材页扫描直接跳过消息预检；普通策略扫描仍可将消息面用于候选股买入前风险检查，但不参与题材识别。题材上下文升级为 v10，旧 v9 上下文不参与跨日确认；严格前向与管理员回测协议分别升级为 `niuone-strict-forward-v29` 和 `niuone-backtest-v30`，部署前须归档旧锁、报告和回测结果。
v30 在候选标签之间加入 20 个交易日的市场中性化收益波形归因：先从个股和同题材其他成员的日收益中扣除全市场中位收益，再计算排除自身后的题材中位波形相关性，并按同一股票全部 `f103` 候选中的相对排名收缩。这样可抑制“5/20 日方向相同但逐日波形无关”的宽泛标签。牛牛的完整研究、分钟刷新和普通策略扫描均不发起消息检索或大模型调用；题材上下文升级为 v11，专用缓存升级为 v9，严格前向与管理员回测协议分别升级为 `niuone-strict-forward-v30` 和 `niuone-backtest-v31`。
v31 修复多概念龙头被 softmax 份额重复惩罚的问题：归因权重继续用于题材广度、成交额、集中度和普通分支过滤，但每只股票归因分最高且不低于 60 的首要题材，即使因候选标签较多而份额低于 15%，仍可参与该题材的龙头资格。资格确定后，结构龙头按个股原始强度、今日龙头按当日涨幅排序，归因分仅作同值次序，不再使用“强度/涨幅 × 归因权重”二次稀释。回测以真实次日开盘校验结构止损资格，5bp 模拟滑点只进入成交价和风险定仓，避免把边界内开盘误判为超限。生命周期、买点、结构止损、涨停和组合风险门槛不变；题材上下文/专用缓存升级为 v12/v10，严格前向/管理员回测协议升级为 `niuone-strict-forward-v31`/`niuone-backtest-v32`。
v32 将个股资金参与从弱加分升级为成熟主线路径的确定性资格：强势分中的 5 日强度/成交额分位权重调整为 20%/15%，并新增由全市场成交额分位、题材内成交额分位和量能参与构成的活跃度分。领涨、转强和启动要求全市场成交额分位不低于 60、题材内不低于 50；试仓保留早期发现能力，但活跃度不足只允许轻仓试仓。规则不按市值大小直接加分，也不以高换手替代真实成交额。题材上下文/专用缓存升级为 v13/v11，严格前向候选证据升级为 v2，协议为 `niuone-strict-forward-v32`；管理员回测协议为 `niuone-backtest-v33`。
财经快讯的重要信息默认通过 `NEWSNOW_DECISION_ENABLED` 进入模拟买卖决策情报包，也可在“财经快讯”设置中关闭。交易日 15:00 前发布的条目归属当日，15:00 后及休市日条目归属下一交易日；只接受具备可靠发布时间的上游重要标记。完整来源、时间、归属交易日、当日/次日角色与陈旧状态随 `decision_intelligence` 耐久保存。快讯只辅助已有候选的 BUY/SELL/HOLD，不新增候选、不改变题材归因，也不绕过资格、仓位和风控。
协议指纹还覆盖预检/盘后 Cron、耐久成交数据库与恢复状态的有效路径，以及调度、存储和评估源码；这些值只保存 SHA-256。`--as-of` 只控制报告截点，协议锁的冻结/重冻资格始终使用实际墙钟日期，不能用回填日期跨过队列起始日改写原锁。
v33 仅调整模型提示词和面向用户的策略理由展示：内部生命周期、个股角色和主线模式继续使用稳定英文枚举，只有明确处于中文策略上下文的独立小写枚举会转换为中文；专名、技术术语、错误文本、缩写和标识符保持原文，二次取舍的嵌套放弃理由也在落盘前规范化。策略评分、资格、仓位和风控不变。由于提示词、决策持久化和展示映射都属于严格前向证据链，协议升级为 `niuone-strict-forward-v33`，默认新队列从 `2026-08-13` 开始；部署前须归档 v32 协议锁和报告，不得混算两个队列。
管理员回测 v34 将信号区间结束后的最终平仓日纳入权益曲线和风险指标，并按近期较慢交易日校正回放预计剩余时间。运行页使用单调时钟持续显示当前交易日耗时，同时锁定参数控件并展示任务实际提交的日期、复权和行情源，避免页面表单与正在执行的任务混淆；策略规则、成交精度和资金计算不变。
v34 取消牛牛上午/下午、单轮及单日新开仓数量限制，只保留最多 5 只持仓和原单笔/组合/主题风险预算。满仓时以注册战法确定性、当前信号分、主线阶段/分数及强势龙头身份计算可审计优先级；只有新候选严格高于可按 T+1 整仓卖出的最低优先级牛牛持仓，才按先卖后买执行换仓。严格前向协议升级为 `niuone-strict-forward-v34`，默认新队列从 `2026-08-19` 开始；管理员回测同步升级为 `niuone-backtest-v35`，旧锁、报告和回测不得混算。
v35 增加同股“评分阶梯”加仓：每次实际 BUY 都记录本次评分、持仓期最高买入评分和成交次数；同一战法再次出现 BUY 时，只有评分严格刷新历史买入最高分才获得加仓资格。试仓仍不得当日重复买入或向亏损仓补仓，成熟路径仍须处于主升、保持强势领涨且浮盈位于 2%～12%；阶段升级、真实减仓后的波段回补以及所有单票/主题/组合风险上限保持独立有效。严格前向协议升级为 `niuone-strict-forward-v35`，管理员回测同步升级为 `niuone-backtest-v36`；默认队列起始日仍为 `2026-08-19`，因为该队列尚未开始。
v36 将盘面总结/评价与牛牛开仓数量彻底解耦。盘面产生的动态持仓数、单轮新仓数和暂停字段不再压缩或清零牛牛开仓，模型提示、二次取舍和成交复核统一只使用最多 5 只持仓及满仓优先级换仓；盘面仍参与单笔/组合/主题风险预算、总仓、现金与候选自身复合硬停止。严格前向协议升级为 `niuone-strict-forward-v36`，管理员回测仍为已采用相同容量语义的 `niuone-backtest-v36`，默认队列日期保持 `2026-08-19`。
v37 将消息面预检的缺失状态与买卖权重解耦：失败、超时、未检查、待判断或不可用的记录不再进入决策消息证据，候选摘要统一按中性、权重 0 处理，不得因此降分、降优先级、缩仓或作为不开仓/HOLD/SELL 的理由；已完成的有效利好、利空或中性结果仍正常参与决策。该提示词变更使严格前向协议升级为 `niuone-strict-forward-v37`；管理员回测不使用该模型消息权重，仍保持 `niuone-backtest-v36`，尚未开始的默认队列日期仍为 `2026-08-19`。
v38 解除牛牛试仓每日最多 2 个候选和同板块/同题材最多 2 只持仓的固定数量限制。候选仍按分数进入全局候选池，持仓仍受最多 5 只、单票、主题风险、主题敞口、组合风险、总仓、现金和 T+1 约束；板块潮汐的同一行业 2 只限制不变。严格前向/管理员回测协议升级为 `niuone-strict-forward-v38`/`niuone-backtest-v37`，新队列从 `2026-08-21` 开始。
v39 把共享盘面上下文里的午盘持仓数和保留名额明确限定为非牛牛规则，并禁止模型用这些字段将未满 5 只的牛牛账户判定为满仓。严格前向协议升级为 `niuone-strict-forward-v39`，管理员回测保持 `niuone-backtest-v37`，新队列从 `2026-08-24` 开始。
v40 将牛牛试仓绝对上限提高到 10%，轮动试仓单笔权益风险与单主题风险同步提高到 1%。满足跨日主升、强势领涨且浮盈处于 2%～12% 的持仓，由本地规则确定性生成 10%/20% 分级加仓；明确 SELL 仍退出优先，所有风险、敞口、现金和 T+1 边界继续执行。严格前向/管理员回测协议升级为 `niuone-strict-forward-v40`/`niuone-backtest-v38`，队列日期保持 `2026-08-24`。
v41 增加默认关闭的持仓快周期：`screening/holding_cycle.py` 只重评模拟账户当前持仓，复用完整扫描的当前策略评分器和最新有效上下文，再回到 Dashboard 的统一盘面总结、模型决策及 `trading/practice_trader.py` 的退出优先与成交复核。快周期 BUY 只允许对执行时仍存在的仓位加仓，不能发现、首次买入或卖后回补新仓；行情或日 K 缺失时该股不给予 BUY 资格但仍执行正常 SELL/HOLD。开关和 60～900 秒间隔运行时热生效，快慢周期共享账户决策锁。严格前向升级为 `niuone-strict-forward-v41`，管理员日线回测保持 `niuone-backtest-v38`，新队列从 `2026-08-27` 开始。
v42 将牛牛持仓只数上限与设置中的 `DASHBOARD_MAX_OPEN_POSITIONS` 统一。模型提示、剩余槽位、满仓换仓、成交复核、管理员回测和严格前向协议均冻结同一个配置值；午盘前上限和盘面动态持仓数仍只约束非牛牛策略，单票、主题、组合、总仓、现金与 T+1 风控不变。严格前向/管理员回测协议升级为 `niuone-strict-forward-v42`/`niuone-backtest-v39`，默认队列仍从 `2026-08-27` 开始。
v43 将非结构性卖出统一为分段软退出：未兑现、卖出评分、题材/行业转弱和普通盈利回撤首次只减仓 50%，余仓至少等待下一交易日确认；4～5 分防卖飞评分首日否决，结构止损、主线失活和市场硬停止仍即时清仓。满仓换仓要求新候选优先级至少高出 3 分。每笔 SELL 以本地日 K 持久计算 1/3/5/10 日 MFE、MAE、收盘/基准超额和换仓相对收益，5 日完整窗口再标记卖飞或避免续亏；软退出清仓后 5 个交易日内，完整扫描只有在站回退出高点/BBI、量能恢复且原题材逻辑有效时才可重开。严格前向/管理员回测协议升级为 `niuone-strict-forward-v43`/`niuone-backtest-v40`，新队列从 `2026-08-28` 开始；部署前归档 v42 锁、报告和旧回测结果。
v44 将 5 日卖后复盘接成默认关闭、可在设置页启用的受约束自动反馈闭环。达到至少 30 个完整样本、覆盖 3 个月后，系统每新增 10 个完整样本最多移动一个预声明档位，只允许调整软退出确认次数/首次减仓比例、满仓换仓优先级差和卖后再入量能门槛；结构止损、账户/组合/主题风险、仓位上限和 T+1 永不参与调参。每笔成交保存算法、参数和版本，SQLite 原子激活不可变版本；版本样本的综合后悔分与卖飞收益显著恶化时自动恢复上一参数。严格前向升级为 `niuone-strict-forward-v44`，管理员回测仍固定 v40 默认规则，新队列从 `2026-08-31` 开始；部署前归档 v43 协议锁和报告。
v45 将自动反馈升级为资金与数据质量优先的 v2：已完成复盘只允许前进，行情短缺不得降级覆盖；收益锚定真实卖出价，换仓只使用实际 BUY，直接再入的放行与拦截都形成 5 日影子样本。同股同日相关成交按簇去重，最近 120 个样本按成交资金加权并使用 90% 置信区间；没有参数变化的 hold 只写评估检查点，不再生成策略版本。SQLite 活动版本会在账户状态加载时对账，算法、配置、前版和提案共同进入指纹；硬止损与全部组合风险仍冻结。严格前向升级为 `niuone-strict-forward-v45`，新队列仍从 `2026-08-31` 开始。
v46 默认开启受约束的 5 日卖后自动调参；未显式配置的部署会在下一轮盘后复盘开始学习或评估，显式设置 `DASHBOARD_EXIT_FEEDBACK_AUTO_TUNE_ENABLED=0` 仍可关闭。所有样本门槛、有界参数网格、自动回滚和冻结风控不变；严格前向升级为 `niuone-strict-forward-v46`，队列仍从 `2026-08-31` 开始。
v47 修复首次盘后复盘前的可观测性：运行配置成为启用状态的权威来源，旧账户快照不能把默认开启误显示为关闭；API 会返回“样本积累中/等待首次盘后复盘”，模拟交易页始终保留卖后 5 日复盘区域。严格前向升级为 `niuone-strict-forward-v47`，调参门槛和冻结风控不变。
v48 修复持仓快周期误用压缩主线缓存的问题：分钟级题材与市场状态继续取最新专用缓存，同日个股评分画像从完整扫描缓存补齐；跨日或缺失画像仍失败关闭，评分异常按类型计数而不再静默伪装成空候选。加仓资格和全部成交风控不变，严格前向升级为 `niuone-strict-forward-v48`，新队列从 `2026-09-03` 开始。

v49 为牛牛试仓首次或清仓后重新开仓增加成交涨幅门：相对有效前收盘价必须严格小于 3%，报价无效同样等待；不接受扫描价格或涨幅替代，后续回落可重新评估。已有持仓加仓、其他入口和退出风控保持原规则。生产成交与默认管理员回测共用纯函数，日线回测用信号日收盘价和含滑点的次日执行价；显式研究跳空参数仍单独标识其研究口径。严格前向/管理员回测升级为 `niuone-strict-forward-v49`/`niuone-backtest-v41`，新队列从 `2026-09-08` 开始。部署前归档旧锁、报告和回测结果；保留真实持仓与成交，带仓基线不能用于新版本账户收益归因。该门针对近期亏损分组提出，仍需后续样本验证，不能把同样本筛选视为胜率提升证明。

v50 统一牛牛试仓、启动、领涨、转强及所有加仓的个股活跃度资格：当日实际累计换手率至少 3%、全市场成交额分位至少 60、动作所选题材内成交额分位至少 50，必需数据无效即暂停买入；取消试仓豁免和旧候选的可选绕过。成交前重新读取报价复核换手率，持仓快周期使用最新换手率与最近完整扫描的成交额排名。早盘未达 3% 时继续等待，不以预计全天换手替代，也不把高换手直接视作买点。首次建仓耐久证据新增 `entry_turnover_pct`；保留 v49 的试仓首次建仓涨幅小于 3% 规则，这两个百分比含义不同。日线回测按信号日已发生的换手率筛选，绝不读取次日全天换手率；次日开盘成交因此只是盘中规则的近似，历史换手缺失会拒绝入选。严格前向/管理员回测升级为 `niuone-strict-forward-v50`/`niuone-backtest-v42`；尚未开始的队列仍从 `2026-09-08` 起算。部署前归档旧协议锁、报告与回测结果，保留账户、持仓和成交历史。3% 是本次采用的活跃度偏好门槛，需通过新队列验证收益、回撤和机会数量，不能据此承诺胜率提升。

v51 统一 Dashboard 策略表现与严格前向评估的完整交易计算：由 `trading/lifecycles.py` 按可核验的零仓到零仓合并加仓和分批卖出，按首次入口归因，扣除全部买卖费用；未清仓、孤立卖出或数量链不完整的生命周期不计入分母，盈亏平衡计入分母但不计获胜。Dashboard 优先读取 SQLite 完整耐久账本并合并近期状态，数据库不可读时将胜率标为不可用；单次卖出另称“盈利卖出占比”。决策槽统计只覆盖当前队列日期，缺失槽按模型失败、payload 缺失、候选证据无效或无耐久记录分类，不把失败/中间退出记录补成成功决策。严格前向升级为 `niuone-strict-forward-v51`，管理员回测保持 `niuone-backtest-v42`；未开始的队列仍从 `2026-09-08` 起算，部署前归档 v50 锁与报告，保留账户历史。试仓价格门的预先固定影子对照规则见 [试仓追涨前向对照](../docs/strategies/PROBE_CHASE_FORWARD.md)。

`entrypoints/` 中的脚本是项目支持的启动路径。`compat/` 通过 `_compat.py` 在历史模块命名空间中运行迁移后的实现，供内部裸模块导入和迁移期集成使用。新增业务代码不得写入 `entrypoints/` 或 `compat/`。

Dashboard 继续由 `entrypoints/niuone_dashboard.py` 单端口启动并保留原页面布局。`dashboard/fastapi_app.py` 只负责 FastAPI/Uvicorn 应用组合、中间件、Vue 构建和共享缓存响应，具体 HTTP 接口按 system、messages、market、practice、admin 拆在 `dashboard/routers/`。旧 `BaseHTTPRequestHandler`、`ThreadingHTTPServer` 及原生 HTML/JavaScript 控制器已经删除。`dashboard/security.py`、`dashboard/visit_stats.py` 和 `dashboard/response_cache.py` 分别承载管理员访问控制、访问统计和并发响应缓存，`dashboard/server.py` 保留后台状态、配置、数据源、行情采样和实战计划的组合实现，但不再定义 HTTP 路由。浏览器展示模型由 `dashboard/public_projection.py` 构建，并由 `dashboard/public_snapshots.py` 原子发布；交易和外部请求不得下沉到前端。

高频投影只读取 `practice_candidates_latest.json`、`today_candidates_latest.json` 与 `niuone_mainline_summary_latest.json` 等有界快照，并按文件身份、大小和纳秒修改时间复用已解析对象。“今日候选股”首次读取当天最多 12 轮扫描归档，按股票去重、保留当日最佳达标记录后原子生成小型汇总，同时分别提供最新一轮当前达标数和今日累计曾达标数；后续仅在归档版本变化时重建。候选页通过单个批量接口按需加载逐股分时缩略图，服务端限制候选数、并发数和总等待时间，并让单只行情失败独立降级。完整候选扫描与逐股题材归因仍保留在原私有缓存中供交易和跨日确认使用；升级后若小型快照尚不存在，Dashboard 只兼容读取一次旧缓存并原子补建，不删除或覆盖完整研究数据。
